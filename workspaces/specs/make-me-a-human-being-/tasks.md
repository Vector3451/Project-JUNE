1. Create a new Python class `Human` in `models/human.py` with attributes for physical appearance, personality traits, and emotional state.
2. Implement a `generate_appearance` method in `models/human.py` to randomly assign physical characteristics such as height, weight, and facial features.
3. Develop a `personality` module in `models/personality.py` to define and generate personality traits based on the Big Five personality traits model.
4. Integrate the `personality` module into the `Human` class in `models/human.py` to assign personality traits to the simulated human.
5. Create an `emotions` module in `models/emotions.py` to define and manage emotional states and responses.
6. Implement emotional responses to basic user interactions in the `Human` class in `models/human.py` using the `emotions` module.
7. Design a basic user interface in `views/human_view.py` to display the simulated human's physical appearance and emotional state.
8. Integrate the `Human` class with the user interface in `views/human_view.py` to enable user interaction with the simulated human.