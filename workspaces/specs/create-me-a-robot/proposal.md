Create a basic robot simulation with a graphical user interface (GUI) to demonstrate its movement and interaction capabilities. The robot will be able to move forward, backward, left, and right within a bounded 2D space. The GUI will display the robot's current position and orientation. We will use Python as the programming language and the Pygame library for the GUI. The robot's movement will be controlled by keyboard input.