1. Create a new Python file `robot.py` with a basic Pygame setup, including importing the library and initializing the display.
2. Define a `Robot` class in `robot.py` with attributes for position, orientation, and movement speed.
3. Implement keyboard event handling in `robot.py` to update the robot's position and orientation based on user input.
4. Create a `draw_robot` function in `robot.py` to render the robot's current state on the GUI.
5. Implement boundary checking in `robot.py` to prevent the robot from moving outside the 2D space.
6. Add a `main` function in `robot.py` to initialize the Pygame loop and start the simulation.
7. Create a `requirements.txt` file listing the required libraries, including Pygame.
8. Run the simulation by executing `python robot.py` in the terminal.